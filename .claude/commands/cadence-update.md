---
description: "Update Cadence to the latest version from GitHub"
disable-model-invocation: true
allowed-tools: Bash
---
Run this exact command to update Cadence. Do not read any files first.
Do not search the codebase. Do not provide commentary before running
the command. Just execute it:

curl -sL https://raw.githubusercontent.com/dizk/cadence/main/install.sh | bash

After the command completes, report only: "Cadence updated." and the
output from the installer.
